import { createContext } from "react";
//first step
// create a context with createContext method and export it.
//second step => create a provider 
const UserContext = createContext();
export default UserContext;
